'use client';
import { ArrowRight, CheckCircle2, MessageCircle } from 'lucide-react';
import { copy, Lang } from '@/data/content';
import { whatsappLink, packageMessage } from '@/lib/whatsapp';
export default function Hero({lang}:{lang:Lang}){const c=copy[lang];return <section className="mx-auto grid max-w-7xl items-center gap-10 px-4 pb-20 pt-16 md:grid-cols-2 md:pt-24" dir={lang==='ar'?'rtl':'ltr'}><div className="text-center md:text-start"><span className="inline-flex rounded-full bg-white/80 px-4 py-2 text-sm font-bold text-violet shadow-sm">✦ {c.heroBadge}</span><h1 className="mt-7 text-4xl font-black tracking-tight text-ink md:text-6xl">{c.heroTitle}</h1><p className="mx-auto mt-6 max-w-2xl text-base leading-8 text-ink/65 md:mx-0">{c.heroText}</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row md:justify-start"><a href={whatsappLink(packageMessage())} className="inline-flex items-center justify-center gap-2 rounded-full bg-violet px-7 py-4 font-bold text-white shadow-lg shadow-violet-300/60"><MessageCircle size={18}/>{c.cta}</a><a href="#pricing" className="inline-flex items-center justify-center gap-2 rounded-full bg-white px-7 py-4 font-bold text-ink shadow-sm border border-violet-100">{c.viewPackages}<ArrowRight size={18}/></a></div></div><div className="relative min-h-[520px]"><div className="absolute left-2 top-6 w-72 rounded-[30px] bg-white p-5 shadow-soft"><p className="text-sm font-bold">Company Type</p>{['LLC','C-Corp','S-Corp'].map((x,i)=><div key={x} className="mt-4 flex items-center gap-3 rounded-2xl border border-violet-100 p-3"><span className={`h-3 w-3 rounded-full ${i===0?'bg-violet':'bg-violet-200'}`}/><span className="text-sm font-semibold">{x}</span></div>)}</div><div className="absolute right-0 top-0 w-72 rounded-[30px] bg-[#2B1688] p-5 text-white shadow-soft"><p className="text-sm opacity-80">Timeline</p><h3 className="mt-2 text-2xl font-black">Formation Journey</h3>{['Choose Package','WhatsApp Onboarding','Document Checklist','Launch Ready'].map((x)=><div key={x} className="mt-4 flex items-center gap-3"><CheckCircle2 size={18}/><span className="text-sm">{x}</span></div>)}</div><div className="absolute bottom-8 left-10 w-[330px] rounded-[34px] bg-white p-6 shadow-soft"><p className="text-sm font-bold">Growth Formation</p><div className="mt-2 text-4xl font-black">$399</div><p className="mt-3 text-sm text-ink/60">Most popular setup for founders.</p><div className="mt-5 h-3 rounded-full bg-lavender"><div className="h-3 w-2/3 rounded-full bg-violet"/></div></div><div className="absolute bottom-0 right-8 w-64 rounded-[26px] bg-white/80 p-4 shadow-soft backdrop-blur"><div className="flex items-center gap-3"><MessageCircle className="text-violet"/><div><p className="font-bold">WhatsApp Support</p><p className="text-xs text-ink/60">Manual onboarding after CTA.</p></div></div></div></div></section>}
